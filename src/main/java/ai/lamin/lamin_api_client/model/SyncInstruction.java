/*
 * FastAPI
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package ai.lamin.lamin_api_client.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import ai.lamin.lamin_api_client.JSON;

/**
 * Defines a single synchronization instruction, containing the scope and the desired state for that scope.
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2026-07-28T08:39:36.641259014+02:00[Europe/Brussels]", comments = "Generator version: 7.23.0")
public class SyncInstruction {
  public static final String SERIALIZED_NAME_SCOPE = "scope";
  @SerializedName(SERIALIZED_NAME_SCOPE)
  @javax.annotation.Nonnull
  private Map<String, Object> scope = new HashMap<>();

  public static final String SERIALIZED_NAME_DESIRED_IDS = "desired_ids";
  @SerializedName(SERIALIZED_NAME_DESIRED_IDS)
  @javax.annotation.Nonnull
  private List<Object> desiredIds = new ArrayList<>();

  public SyncInstruction() {
  }

  public SyncInstruction scope(@javax.annotation.Nonnull Map<String, Object> scope) {
    this.scope = scope;
    return this;
  }

  public SyncInstruction putScopeItem(String key, Object scopeItem) {
    if (this.scope == null) {
      this.scope = new HashMap<>();
    }
    this.scope.put(key, scopeItem);
    return this;
  }

  /**
   * Key-value pairs defining the constant scope for this specific synchronization operation (e.g., {&#39;artifact_id&#39;: 1, &#39;feature_id&#39;: 10}).
   * @return scope
   */
  @javax.annotation.Nonnull
  public Map<String, Object> getScope() {
    return scope;
  }

  public void setScope(@javax.annotation.Nonnull Map<String, Object> scope) {
    this.scope = scope;
  }


  public SyncInstruction desiredIds(@javax.annotation.Nonnull List<Object> desiredIds) {
    this.desiredIds = desiredIds;
    return this;
  }

  public SyncInstruction addDesiredIdsItem(Object desiredIdsItem) {
    if (this.desiredIds == null) {
      this.desiredIds = new ArrayList<>();
    }
    this.desiredIds.add(desiredIdsItem);
    return this;
  }

  /**
   * The complete list of desired IDs for the list_key within this scope.
   * @return desiredIds
   */
  @javax.annotation.Nonnull
  public List<Object> getDesiredIds() {
    return desiredIds;
  }

  public void setDesiredIds(@javax.annotation.Nonnull List<Object> desiredIds) {
    this.desiredIds = desiredIds;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    SyncInstruction syncInstruction = (SyncInstruction) o;
    return Objects.equals(this.scope, syncInstruction.scope) &&
        Objects.equals(this.desiredIds, syncInstruction.desiredIds);
  }

  @Override
  public int hashCode() {
    return Objects.hash(scope, desiredIds);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class SyncInstruction {\n");
    sb.append("    scope: ").append(toIndentedString(scope)).append("\n");
    sb.append("    desiredIds: ").append(toIndentedString(desiredIds)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    return o == null ? "null" : o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>(Arrays.asList("scope", "desired_ids"));

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>(Arrays.asList("scope", "desired_ids"));
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to SyncInstruction
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!SyncInstruction.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "The required field(s) %s in SyncInstruction is not found in the empty JSON string", SyncInstruction.openapiRequiredFields.toString()));
        }
      }

      Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
      // check to see if the JSON string contains additional fields
      for (Map.Entry<String, JsonElement> entry : entries) {
        if (!SyncInstruction.openapiFields.contains(entry.getKey())) {
          throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "The field `%s` in the JSON string is not defined in the `SyncInstruction` properties. JSON: %s", entry.getKey(), jsonElement.toString()));
        }
      }

      // check to make sure all required properties/fields are present in the JSON string
      for (String requiredField : SyncInstruction.openapiRequiredFields) {
        if (jsonElement.getAsJsonObject().get(requiredField) == null) {
          throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "The required field `%s` is not found in the JSON string: %s", requiredField, jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      // ensure the required json array is present
      if (jsonObj.get("desired_ids") == null) {
        throw new IllegalArgumentException("Expected the field `linkedContent` to be an array in the JSON string but got `null`");
      } else if (!jsonObj.get("desired_ids").isJsonArray()) {
        throw new IllegalArgumentException(String.format(java.util.Locale.ROOT, "Expected the field `desired_ids` to be an array in the JSON string but got `%s`", jsonObj.get("desired_ids").toString()));
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!SyncInstruction.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'SyncInstruction' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<SyncInstruction> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(SyncInstruction.class));

       return (TypeAdapter<T>) new TypeAdapter<SyncInstruction>() {
           @Override
           public void write(JsonWriter out, SyncInstruction value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public SyncInstruction read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of SyncInstruction given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of SyncInstruction
   * @throws IOException if the JSON string is invalid with respect to SyncInstruction
   */
  public static SyncInstruction fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, SyncInstruction.class);
  }

  /**
   * Convert an instance of SyncInstruction to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

